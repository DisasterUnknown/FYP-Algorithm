# Controller values
ALPHA = 0.4     # controls how strong the architectural scales influence the migration effort
DELTA = 3       # controls how early the q-day might arrive (int 0 < delta < Z)
GAMMA = 1.6     # controls the urgency curve

# Algorithm values
Z = 10          # estimated number years until the system becomes unmaintainable (Q-day)